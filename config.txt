# ============================================================
# Global Doodle Configuration
# Contains GitHub settings shared by all doodles.
# The GitHub token is NOT stored here — it lives in your browser's
# localStorage and is never committed to the repository.
# Use setup.html to enter your token.
# ============================================================

[github]
owner = pederbacher
repo = doodle
branch = main
